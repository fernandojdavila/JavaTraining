package pooling;

public class StackAndHeap {

    // Heap
    /*
    Stores instances and arrays, also called share memory as this is the place
    where multiple threads will share same data.

     */



}
